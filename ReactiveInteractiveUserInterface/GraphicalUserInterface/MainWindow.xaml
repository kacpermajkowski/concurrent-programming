<Window x:Class="TP.ConcurrentProgramming.PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TP.ConcurrentProgramming.Presentation.ViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        xmlns:v="clr-namespace:TP.ConcurrentProgramming.PresentationView"
        mc:Ignorable="d"
        Title="Presentation layer targeting reactive behaviour"
        Width="{Binding WindowWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
        Height="{Binding WindowHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
        >
    <Window.Resources>
        <v:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <StackPanel>
        <Label Content="Wprowadź liczbę kul:"
               Visibility="{Binding SettingsVisibility, Converter={StaticResource BoolToVis}}"
               HorizontalAlignment="Center" Margin="0,20,0,10.3"/>
        <TextBox TextWrapping="Wrap" 
                 Visibility="{Binding SettingsVisibility, Converter={StaticResource BoolToVis}}"
                 Text="{Binding BallCountText, UpdateSourceTrigger=PropertyChanged}" 
                 Width="60"/>
        <Button 
            Visibility="{Binding SettingsVisibility, Converter={StaticResource BoolToVis}}"
            Command="{Binding StartCommand}"
            Content="Rozpocznij" Width="100" Margin="0,10,0,30"/>
        <Border BorderThickness="4" BorderBrush="Green" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl ItemsSource="{Binding Balls}" Width="400" Height="420">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width= "{Binding Diameter}" Height="{Binding Diameter}" Fill="Blue" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="{x:Type ContentPresenter}">
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>
    </StackPanel>
</Window>